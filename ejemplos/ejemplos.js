var todos = ['Gandalf', 'Legolas', 'Peron']

todos.forEach(todos => { 
  var div = document.createElement('Div');
  div.innerText = todos;
  document.body.appendChild(div);

})
// con esto listo todos los pjs 